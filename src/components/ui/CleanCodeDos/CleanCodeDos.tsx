// import { MIN_RULE_LENGTH } from "./constants/rules.js";

function CleanCodeDos() {
  const value = "";
  if (value.length > 3) {
    // if (value.length > MIN_RULE_LENGTH) {
    console.log("Prueba");
  }
  return <div>CleanCodeDos</div>;
}

export default CleanCodeDos;
